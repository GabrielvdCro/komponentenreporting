/* ============================================================
   HINWEIS ZUR CODEERSTELLUNG
   ============================================================
   Die SQL- und Python-Codes werden mit Unterstützung
   künstlicher Intelligenz entwickelt. Sämtliche generierten
   Codes werden kritisch geprüft und bei Bedarf angepasst.
   ============================================================ */


/* ============================================================
   Datenbank und Schemas erstellen
   ============================================================
   Erstellt die Datenbank Komponentenreporting sowie die
   Schemas Bronze, Silver und Gold für die Datenpipeline.
   ============================================================ */


-- Datenbank erstellen
CREATE DATABASE IF NOT EXISTS Komponentenreporting;


-- Datenbank auswählen
USE Komponentenreporting;


-- Schemas erstellen
CREATE SCHEMA IF NOT EXISTS bronze;

CREATE SCHEMA IF NOT EXISTS silver;

CREATE SCHEMA IF NOT EXISTS gold;
